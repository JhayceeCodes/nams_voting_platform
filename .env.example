# ─── SECURITY ───────────────────────────────────────────────
SECRET_KEY=
DEBUG=

# ─── DATABASE ───────────────────────────────────────────────
# Render provides this automatically, but you can set one for local testing
# Example for local PostgreSQL: 
# DATABASE_URL=postgres://USER:PASSWORD@localhost:5432/yourdbname
DATABASE_URL=

# ─── RENDER SETTINGS ────────────────────────────────────────
# This is injected automatically on Render
RENDER_EXTERNAL_HOSTNAME=

# ─── FRONTEND / CORS SETTINGS ───────────────────────────────
# Comma-separated list of allowed frontend URLs
# Example for Render + localhost testing:
FRONTEND_URLS=

# ─── DJANGO SETTINGS ────────────────────────────────────────
# Path to your production settings file
DJANGO_SETTINGS_MODULE=

# ─── JWT SETTINGS (Optional if using SimpleJWT) ─────────────
ACCESS_TOKEN_LIFETIME_MINUTES=
REFRESH_TOKEN_LIFETIME_DAYS=

